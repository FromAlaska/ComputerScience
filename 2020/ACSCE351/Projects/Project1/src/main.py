#!/usr/bin/env python3

import sys

buffArray = []
state = None

STATE_ADD = None

#Basic Parsing Algorithm
def parse(inputString):
    print("Input: " + inputString)

    for totalNum in inputString:
        #buffArray.append(totalNum)
        if(totalNum ==):
            print("stuff")

    print(buffArray)

if __name__ == "__main__":
    stringInput = input("Enter the stuff: ")
    parse(stringInput)

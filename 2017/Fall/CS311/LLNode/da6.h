// da6.h
// Jim Samson
// 08 November 2017
//
// For CS311 Fall 2017

#include "llnode2.h"

// reverseList(shared_ptr<LLNode2<ValType>>);
// This function is given a shared_pt to a Linked List-an empty shared_ptr if the
// List is empty. It alters the list so that the values appear in reverse order.
// A pointer  to the new list is reterned via the reference parameter.
template<typname ValType>
void reverseList(shared_ptr<LLNode2<Valtype>> &head) noexcept
{

}

template<typename ValType>
class LLMap {
 LLMap()
public:


private:
};

